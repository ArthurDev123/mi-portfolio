<section id="buttons">
  <button
    ><svg
      xmlns="http://www.w3.org/2000/svg"
      width="22"
      height="22"
      fill="currentColor"
      class="bi bi-send"
      viewBox="0 0 16 13"
    >
      <path
        d="M15.854.146a.5.5 0 0 1 .11.54l-5.819 14.547a.75.75 0 0 1-1.329.124l-3.178-4.995L.643 7.184a.75.75 0 0 1 .124-1.33L15.314.037a.5.5 0 0 1 .54.11ZM6.636 10.07l2.761 4.338L14.13 2.576zm6.787-8.201L1.591 6.602l4.339 2.76z"
      ></path>
    </svg> Contactame</button
  >
  <a
    href="/images/Copia de Black And White Minimalist Professional Resume A4.pdf"
    download
    ><button
      ><svg
        xmlns="http://www.w3.org/2000/svg"
        width="22"
        height="22"
        fill="currentColor"
        class="bi bi-download"
        viewBox="0 0 16 16"
      >
        <path
          d="M.5 9.9a.5.5 0 0 1 .5.5v2.5a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1v-2.5a.5.5 0 0 1 1 0v2.5a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2v-2.5a.5.5 0 0 1 .5-.5"
        ></path>

        <path
          d="M7.646 11.854a.5.5 0 0 0 .708 0l3-3a.5.5 0 0 0-.708-.708L8.5 10.293V1.5a.5.5 0 0 0-1 0v8.793L5.354 8.146a.5.5 0 1 0-.708.708z"
        ></path>
      </svg> Descargar CV</button
    ></a
  >
</section>
<style>
  #buttons {
    margin-top: clamp(1rem, 2vw, 20px);
    display: flex;
    flex-direction: row;
    gap: clamp(0.3rem, 1vw, 1rem);
    flex-wrap: wrap;
    justify-content: center;
  }

  button {
    padding: clamp(0.6rem, 2vw, 24px);
    margin: clamp(0.2rem, 0.5vw, 5px);
    background-color: var(--accent);
    border-style: none;
    border-radius: 5px;
    font-weight: bold;
    font-size: clamp(0.75rem, 1.8vw, 20px);
    cursor: pointer;
    transition: transform 0.3s ease, background-color 0.3s ease;
    display: flex;
    align-items: center;
    gap: clamp(0.3rem, 1vw, 0.8rem);
    white-space: nowrap;
    color: var(--text-primary);
  }

  button:hover {
    transform: scale(1.1);
    background-color: var(--accent-light);
  }

  button:active {
    transform: scale(0.98);
  }

  svg {
    width: clamp(12px, 2.5vw, 22px);
    height: clamp(12px, 2.5vw, 22px);
  }

  a {
    text-decoration: none;
  }

  /* Tablet */
  @media (max-width: 768px) {
    #buttons {
      margin-top: clamp(0.5rem, 1.5vw, 10px);
      gap: clamp(0.2rem, 0.8vw, 0.5rem);
    }

    button {
      padding: clamp(0.4rem, 1.5vw, 1rem);
      margin: clamp(0.1rem, 0.3vw, 3px);
      font-size: clamp(0.65rem, 1.5vw, 1rem);
    }

    svg {
      width: clamp(10px, 2vw, 15px);
      height: clamp(10px, 2vw, 15px);
    }
  }

  /* Mobile */
  @media (max-width: 480px) {
    #buttons {
      margin-top: clamp(0.4rem, 1vw, 8px);
      gap: clamp(0.2rem, 0.6vw, 0.3rem);
    }

    button {
      padding: clamp(0.3rem, 1vw, 0.6rem);
      margin: clamp(0.05rem, 0.2vw, 1px);
      font-size: clamp(0.6rem, 1.2vw, 0.8rem);
    }

    svg {
      width: clamp(8px, 1.5vw, 12px);
      height: clamp(8px, 1.5vw, 12px);
    }
  }
</style>
</style>
