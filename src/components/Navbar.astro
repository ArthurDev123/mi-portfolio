<link
  rel="stylesheet"
  href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=dark_mode,light_mode"
/>

<nav id="navbar">
  <a href="" class="language-toggle">Español/English</a>
  <ul>
    <li><a href="#aboutme">Sobre mí</a></li>
    <li><a href="#projects">Proyectos</a></li>
    <li><a href="#contact">Contactame</a></li>
    <li><span class="material-symbols-outlined" id="themeToggle" role="button" tabindex="0">dark_mode</span></li>
  </ul>
</nav>

<script>
  let lastScrollTop = 0;
  const navbar = document.getElementById('navbar');
  const themeToggle = document.getElementById('themeToggle');

  // Tema mode toggle
  if (themeToggle) {
    themeToggle.addEventListener('click', () => {
      document.documentElement.classList.toggle('light-mode');
      
      if (document.documentElement.classList.contains('light-mode')) {
        themeToggle.textContent = 'light_mode';
        localStorage.setItem('theme', 'light');
      } else {
        themeToggle.textContent = 'dark_mode';
        localStorage.setItem('theme', 'dark');
      }
    });

    // Cargar tema guardado
    const savedTheme = localStorage.getItem('theme') || 'dark';
    if (savedTheme === 'light') {
      document.documentElement.classList.add('light-mode');
      themeToggle.textContent = 'light_mode';
    }
  }

  // Navbar scroll hide
  window.addEventListener('scroll', function() {
    let currentScroll = window.pageYOffset || document.documentElement.scrollTop;
    
    if (navbar) {
      if (currentScroll > lastScrollTop && currentScroll > 80) {
        // Deslizando hacia abajo y hemos pasado los primeros 80px
        navbar.style.transform = 'translateY(-100%)';
      } else {
        // Deslizando hacia arriba
        navbar.style.transform = 'translateY(0)';
      }
    }
    
    lastScrollTop = currentScroll <= 0 ? 0 : currentScroll;
  });
</script>

<style>
  nav {
    display: flex;
    background: transparent;
    padding: clamp(0.5rem, 2vw, 1rem);
    gap: clamp(1rem, 10vw, 30rem);
    font-size: clamp(0.7rem, 2vw, 0.9rem);
    justify-content: center;
    align-items: center;
    width: 100%;
    flex-wrap: wrap;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    backdrop-filter: blur(10px);
    background-color: rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease;
    max-width: 1600px;
    margin: 0 auto;
    box-sizing: border-box;
  }

  .language-toggle {
    white-space: nowrap;
  }

  ul {
    display: flex;
    gap: clamp(0.8rem, 3vw, 1.8rem);
    list-style: none;
    justify-content: flex-end;
    margin: 0;
    padding: 0;
    cursor: pointer;
    flex-wrap: wrap;
    align-items: center;
  }

  .material-symbols-outlined {
    font-size: clamp(18px, 4vw, 24px);
    cursor: pointer;
    transition: color 0.3s ease;
  }

  a {
    color: var(--text-primary);
    text-decoration: none;
    font-weight: bold;
    transition: color 0.3s ease;
  }

  a:hover {
    color: var(--accent);
  }

  li:hover {
    color: var(--accent);
  }

  #themeToggle {
    transition: color 0.3s ease, transform 0.3s ease;
  }

  #themeToggle:hover {
    color: var(--accent);
    transform: rotate(20deg);
  }

  /* Tablet */
  @media (max-width: 768px) {
    nav {
      padding: clamp(0.5rem, 2vw, 1rem);
    }

    ul {
      gap: clamp(0.5rem, 2vw, 1rem);
    }
    .material-symbols-outlined {
      font-size: clamp(16px, 3vw, 20px);
    }
  }

  /* Mobile */
  @media (max-width: 480px) {

    .language-toggle {
      font-size: clamp(0.5rem, 1vw, 0.6rem);
    }
  }
</style>
</style>
