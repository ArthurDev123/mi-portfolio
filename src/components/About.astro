<section id="aboutme">
  <section id="second">
    <h2>Sobre mí</h2>
    <h3>Quién soy</h3>
    <p>
      Estudiante de tercer año de Ingeniería Multimedia, apasionado por el
      desarrollo web, con sólidas habilidades en diseño back-end y front-end.
      Con rápida adaptación a nuevos entornos y gran capacidad para aprender
      nuevos lenguajes y frameworks de programación de forma eficiente. Si tiene
      alguna pregunta o algun comentario no dude en contactarme.
    </p>
  </section>
  <section id="trayectoria">
    <h2>Mi trayectoria</h2>
    <h3>Educación y trabajo</h3>
    <section id="eyt">
      <div class="jobs">
        <img
          class="job_icon"
          src="/images/business_center_40dp_FFFFFF_FILL0_wght400_GRAD0_opsz40.svg"
          alt=""
        />
        <h2>Trabajo</h2>
        <img
          src="/images/web_traffic_30dp_FFFFFF_FILL0_wght400_GRAD0_opsz24.svg"
          alt=""
        />
      </div>
      <div class="studies">
        <img
          class="studies_icon"
          src="/images/school_40dp_FFFFFF_FILL0_wght400_GRAD0_opsz40.svg"
          alt=""
        />
        <h2>Educación</h2>
        <img
          src="/images/web_traffic_30dp_FFFFFF_FILL0_wght400_GRAD0_opsz24.svg"
          alt=""
        />
      </div>
    </section>
    <div class="div_linea-vertical">
      <div class="linea-vertical"></div>
      <span class="linea-vertical-icon"
        ><img
          src="/images/business_center_28dp_FFFFFF_FILL0_wght400_GRAD0_opsz24.svg"
          alt=""
        /></span
      >
    </div>
  </section>
</section>
<style>
  #second p {
    max-width: 100%;
    padding: 0 clamp(1rem, 5vw, 2rem);
  }

  #aboutme {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 0;
    padding: clamp(1rem, 3vw, 2rem);
    width: 100%;
  }

  #second {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
  }

  #second h2 {
    margin: 0;
    font-size: clamp(1.4rem, 5vw, 2.2rem);
  }

  #second h3 {
    margin: clamp(0.5rem, 1vw, 1rem) 0 0 0;
    font-weight: 400;
    color: #d8d8d8;
    font-size: clamp(0.75rem, 2vw, 1rem);
  }

  #trayectoria {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin-top: clamp(1.5rem, 3vw, 30px);
    width: 100%;
    padding: 0 clamp(1rem, 3vw, 2rem);
  }

  #trayectoria h2 {
    margin: 0;
    font-size: clamp(1.4rem, 5vw, 2.2rem);
  }

  #trayectoria h3 {
    margin: clamp(0.5rem, 1vw, 1rem) 0 0 0;
    font-weight: 400;
    color: #d8d8d8;
    font-size: clamp(0.75rem, 2vw, 1rem);
  }

  #eyt {
    display: flex;
    gap: clamp(1rem, 4vw, 28px);
    justify-content: center;
    align-items: center;
    margin-top: clamp(2rem, 4vw, 75px);
    flex-wrap: wrap;
  }

  #eyt h2 {
    font-size: clamp(1rem, 3vw, 1.7rem);
    transition: color 0.3s ease;
  }

  #eyt h2:hover {
    color: #604a84;
    cursor: pointer;
  }

  .jobs,
  .studies {
    display: flex;
    gap: clamp(2px, 1vw, 5px);
    align-items: center;
    flex-direction: column;
  }

  .div_linea-vertical {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: clamp(1rem, 2vw, 50px);
  }

  .linea-vertical {
    width: clamp(1.5px, 0.3vw, 3px);
    height: clamp(150px, 40vw, 210px);
    background-color: #f4f4f4;
  }

  .linea-vertical-icon {
    position: absolute;
    display: flex;
    background-color: #604a84;
    height: clamp(40px, 8vw, 60px);
    width: clamp(40px, 8vw, 60px);
    border-radius: 50%;
    margin-bottom: clamp(2rem, 4vw, 50px);
    align-items: center;
    justify-content: center;
  }

  .linea-vertical-icon img {
    height: clamp(15px, 4vw, 25px);
    width: clamp(15px, 4vw, 25px);
  }

  /* Tablet */
  @media (max-width: 768px) {
    #aboutme {
      padding: clamp(0.5rem, 2vw, 1rem);
    }

    #second p {
      max-width: 90%;
      font-size: clamp(0.75rem, 2vw, 1rem);
      text-align: center;
      margin-top: clamp(1rem, 2vw, 30px);
    }

    #second h2 {
      font-size: clamp(1.2rem, 4vw, 1.5rem);
    }

    #second h3 {
      font-size: clamp(0.65rem, 1.5vw, 0.75rem);
    }

    #trayectoria h2 {
      font-size: clamp(1.2rem, 4vw, 1.5rem);
    }

    #trayectoria h3 {
      font-size: clamp(0.65rem, 1.5vw, 0.75rem);
    }

    #eyt {
      gap: clamp(0.8rem, 3vw, 13px);
      margin-top: clamp(1.5rem, 3vw, 45px);
    }

    #eyt h2 {
      font-size: clamp(0.6rem, 2vw, 0.8rem);
    }

    .jobs,
    .studies {
      gap: clamp(1px, 0.8vw, 3px);
    }

    .linea-vertical {
      height: clamp(120px, 30vw, 220px);
    }

    .linea-vertical-icon {
      height: clamp(35px, 6vw, 40px);
      width: clamp(35px, 6vw, 40px);
      margin-bottom: clamp(1.5rem, 3vw, 50px);
    }

    .linea-vertical-icon img {
      height: clamp(12px, 3vw, 20px);
      width: clamp(12px, 3vw, 20px);
    }
  }

  /* Mobile */
  @media (max-width: 480px) {
    #aboutme {
      padding: clamp(0.3rem, 1.5vw, 0.5rem);
    }

    #second p {
      max-width: 95%;
      font-size: clamp(0.65rem, 1.5vw, 0.8rem);
    }

    #trayectoria {
      padding: 0 clamp(0.5rem, 1vw, 1rem);
    }

    #eyt {
      gap: clamp(0.5rem, 2vw, 8px);
      margin-top: clamp(1rem, 2vw, 30px);
    }

    #eyt h2 {
      font-size: clamp(0.55rem, 1.5vw, 0.65rem);
    }

    .linea-vertical {
      height: clamp(100px, 25vw, 150px);
    }

    .linea-vertical-icon {
      height: clamp(30px, 5vw, 35px);
      width: clamp(30px, 5vw, 35px);
      margin-bottom: clamp(1rem, 2vw, 30px);
    }
  }
</style>
